DISCORD_TOKEN=your_bot_token_here
DISCORD_CLIENT_ID=your_bot_client_id_here
DEVELOPER_IDS=your_user_id_here  # Optional: comma-separated user IDs for developer-only commands
NODE_ENV=production              # Optional: development enables debug logging
DATA_DIR=data                    # Optional: where per-guild config is persisted
